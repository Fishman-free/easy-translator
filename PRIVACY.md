# 隐私声明 · Easy Translator（悬停查词）

**生效日期：2026-09-21**

本扩展是本地优先的浏览器扩展（Edge / Chrome）。它**不收集、不存储、不出售**任何个人信息，
没有账号体系，没有遥测（telemetry），没有广告，也没有任何形式的用户画像。

## 1. 会离开你设备的数据（仅一项，且只有单词本身）

| 场景 | 发送内容 | 目的 | 接收方 |
|---|---|---|---|
| 悬停/查询英文单词（默认引擎） | **被查询的那个英文单词**（例如 `serendipity`） | 获取中文释义、音标、词性、例句 | `https://dict.youdao.com/jsonapi`（有道公开词典接口） |

除此之外，**不发送**页面内容、页面地址、浏览历史、输入内容、设备标识或任何账户信息。

> 如果你在设置中启用「本地小模型」引擎，查词请求将发往**你自己配置的本机服务**
> （默认为 `http://127.0.0.1:11434`，即 Ollama），该请求不会离开你的设备，
> 也不会经过有道或任何第三方服务器。

## 2. 图片取词（可选功能，需你显式授权）

当你授权「所有网站」权限并启用图片取词后：

1. 鼠标在图片上停留约 1.5 秒时，扩展会**截取光标周边的一小块屏幕区域**（默认 460×140 像素）；
2. 这块裁剪图被发送到**你自己运行的本机模型服务**（默认 Ollama）用于识别其中的英文单词；
3. 识别到英文单词时，再把**该单词本身**按第 1 节的方式查词。

裁剪图**不会**被上传到任何第三方服务器；识别不到英文单词时**不会显示任何弹窗**。

## 3. 保存在你本机的数据

| 数据 | 位置 | 用途 | 清除方式 |
|---|---|---|---|
| 扩展设置（触发时长、引擎选择、模型地址等） | 浏览器本地存储（`chrome.storage.local`） | 记住你的偏好 | 扩展设置页「恢复默认设置」 |
| 查词缓存（最近的词条结果） | 浏览器本地存储 | 加速重复查词 | 扩展设置页「清空查询缓存」 |

这些数据**只存在于你的浏览器配置中**，不会同步到任何服务器。卸载扩展即随之删除。

## 4. 权限用途说明

| 权限 | 用途 |
|---|---|
| `storage` | 保存上述设置与缓存（仅本机） |
| `activeTab` | 你点击扩展图标时读取当前标签页地址，用于判断是否为 PDF |
| `webRequest` | **只读**观察响应头的 Content-Type 以识别 PDF 页面，不修改、不阻断、不记录请求 |
| `declarativeNetRequestWithHostAccess` | 仅对本扩展发往 `127.0.0.1` / `localhost` 的请求移除 `Origin` 头，以便访问你本机的模型服务（Ollama 默认拒绝浏览器扩展来源）；经 `initiatorDomains` 限定，不影响任何网页自身的请求 |
| 主机权限 `dict.youdao.com` | 查询单词的中文释义 |
| 主机权限 `127.0.0.1` / `localhost` | 访问你本机运行的 OpenAI 兼容模型服务 |
| 可选权限 `<all_urls>`（需你点击授权） | 图片取词需要的区域截屏；内置 PDF 阅读器读取你打开的 PDF 链接。**不授权时**网页文本取词与在线词典功能完全可用 |

## 5. 儿童隐私

本扩展不面向儿童收集任何数据，也不包含任何需要年龄验证的功能或内容。

## 6. 第三方

- 词典查询使用有道的公开接口。你提交的查询词条受其服务条款约束，详见 <https://dict.youdao.com>。
- 本扩展不含任何分析 SDK、广告 SDK 或跟踪脚本。
- 扩展内不加载远程代码：所有脚本随扩展包分发（内置 PDF 阅读器使用的是随包分发的 pdf.js 官方构建产物）。

## 7. 变更

本声明如有更新，会随扩展版本一起提交到本仓库，并在此页标注新的生效日期。

## 8. 联系方式

如对本声明有疑问，请在项目仓库提交 Issue：
<https://github.com/Fishman-free/easy-translator/issues>
